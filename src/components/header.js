import * as React from "react"
import { Link } from "gatsby"
import ResponsiveMainNavbar from "./Navbar/ResponsiveMainNavbar";

const Header = ({ siteTitle }) => (
  <header>
    <ResponsiveMainNavbar />
  </header>
)

export default Header

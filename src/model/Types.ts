export interface PostsInterface {
    id: number
    title: string,
    description: string,
    date: Date,
    topics: string[],
    contentTypeId: number,
}

export interface PostsScrollInterface {
    id: number
    title: string,
    description: string,
    date: Date,
    topics: string[],
    contentTypeId: number,
}